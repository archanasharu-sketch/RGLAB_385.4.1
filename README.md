# Connecting Python to SQL
- to create a virtual env, terminal command:`python -m venv nameOfEnv`
- activate virtual env
    1.check for bin or scripts inside of env folder
    2. `source nameOfEnv/bin/activate` OR `source nameOfEnv/Scripts/activate` OR `nameOfEnv/bin/activate` OR `nameOfEnv/Scripts/activate


    ## Why we use virtual env
    - create isolated version specific env for projects